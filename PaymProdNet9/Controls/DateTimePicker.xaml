<UserControl x:Class="PaymProdNet9.Controls.DateTimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <DatePicker Grid.Column="0" x:Name="DatePickerControl"
                    SelectedDateChanged="DatePicker_SelectedDateChanged"
                    materialDesign:HintAssist.Hint="Дата" />
        
        <ComboBox Grid.Column="1" x:Name="HoursComboBox"
                  materialDesign:HintAssist.Hint="Час"
                  Width="70"
                  Margin="5,0,2,0"
                  SelectionChanged="TimeSelectionChanged"
                  VerticalAlignment="Center" />
        
        <ComboBox Grid.Column="2" x:Name="MinutesComboBox"
                  materialDesign:HintAssist.Hint="Мин"
                  Width="70"
                  Margin="2,0,0,0"
                  SelectionChanged="TimeSelectionChanged"
                  VerticalAlignment="Center" />
    </Grid>
</UserControl>

